# Сервер токенов Twilio для видеоконференций

## 🚀 Быстрый запуск

### 1. Установка зависимостей
```bash
npm install
```

### 2. Запуск сервера
```bash
# Вариант 1: Через скрипт
./start-token-server.sh

# Вариант 2: Напрямую
node token-server.js

# Вариант 3: В режиме разработки
npm run dev
```

### 3. Проверка работы
Откройте в браузере: http://localhost:3007/info

## 📡 API Endpoints

### GET /token?identity=username
Создает JWT токен для пользователя
- **identity** (query) - имя пользователя
- **Возвращает**: JWT токен для Twilio Video

### GET /info
Информация о сервере
- **Возвращает**: статус сервера и конфигурацию

## 🔧 Конфигурация

Учетные данные Twilio должны быть заданы через переменные окружения (не храните их в коде):
- **Account SID**: TWILIO_ACCOUNT_SID
- **API Key**: TWILIO_API_KEY
- **API Secret**: TWILIO_API_SECRET

Пример `.env` (не коммитить):
```
TWILIO_ACCOUNT_SID=ACxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
TWILIO_API_KEY=SKxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
TWILIO_API_SECRET=your_api_secret_here
```

## 🐛 Устранение проблем

### Ошибка "Cannot find module"
```bash
npm install
```

### Ошибка "Port 3007 already in use"
```bash
# Найти процесс на порту 3007
lsof -i :3007

# Убить процесс
kill -9 <PID>
```

### Ошибка "Invalid Access Token"
1. Проверьте, что сервер запущен на порту 3007
2. Проверьте учетные данные Twilio
3. Убедитесь, что токен не истек (действует 1 час)

## 📝 Логи

Сервер выводит в консоль:
- ✅ Успешное создание токенов
- ❌ Ошибки создания токенов
- 🔥 Статус запуска сервера

## 🔒 Безопасность

⚠️ **ВАЖНО**: Не публикуйте учетные данные Twilio в публичных репозиториях!

Для продакшена:
1. Используйте переменные окружения
2. Настройте HTTPS
3. Добавьте аутентификацию
4. Ограничьте доступ по IP

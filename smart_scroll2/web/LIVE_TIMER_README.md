# Живой таймер Smart Scroll

## 🎯 Новый функционал

Добавлен **живой таймер** на страницу статистики, который показывает в реальном времени, сколько времени пользователь провел на сайте.

## ✨ Особенности

### 🕐 **Живой таймер**
- **Формат времени**: `ЧЧ:ММ:СС` (часы:минуты:секунды)
- **Обновление**: каждую секунду в реальном времени
- **Шрифт**: моноширинный (Courier New) для четкости
- **Размер**: крупный, хорошо видимый

### 🟢 **Индикатор активности**
- **Зеленый пульсирующий кружок** - пользователь активен
- **Красный статичный кружок** - пользователь неактивен
- **Текст статуса**: "Активен" / "Неактивен"

### 🎨 **Дизайн**
- **Градиентный фон**: от зеленого к синему
- **Анимация**: пульсирующее свечение
- **Иконка**: часы в круге
- **Адаптивность**: корректно отображается на мобильных

## 🔧 Технические детали

### HTML структура
```html
<div class="live-timer-container">
    <div class="live-timer">
        <div class="live-timer__icon">...</div>
        <div class="live-timer__content">
            <div class="live-timer__time" id="liveTimer">00:00:00</div>
            <div class="live-timer__label">Время на сайте</div>
            <div class="live-timer__status">
                <span class="status-dot" id="statusDot"></span>
                <span id="statusText">Активен</span>
            </div>
        </div>
    </div>
</div>
```

### JavaScript логика
```javascript
updateLiveTimer() {
    // Форматирование времени в ЧЧ:ММ:СС
    const totalSeconds = Math.floor(this.totalTime / 1000);
    const hours = Math.floor(totalSeconds / 3600);
    const minutes = Math.floor((totalSeconds % 3600) / 60);
    const seconds = totalSeconds % 60;
    
    // Обновление статуса активности
    if (this.isActive) {
        statusDotElement.classList.remove('inactive');
        statusTextElement.textContent = 'Активен';
    } else {
        statusDotElement.classList.add('inactive');
        statusTextElement.textContent = 'Неактивен';
    }
}
```

## 🎨 CSS анимации

### Пульсирующее свечение
```css
@keyframes pulse-glow {
    0%, 100% { 
        box-shadow: 0 8px 32px rgba(48, 202, 161, 0.3);
    }
    50% { 
        box-shadow: 0 12px 40px rgba(48, 202, 161, 0.5);
    }
}
```

### Пульсирующий индикатор
```css
@keyframes pulse-dot {
    0%, 100% { 
        opacity: 1;
        transform: scale(1);
    }
    50% { 
        opacity: 0.7;
        transform: scale(1.2);
    }
}
```

## 📱 Адаптивность

### Десктоп
- Горизонтальное расположение элементов
- Размер таймера: 48px
- Полная функциональность

### Планшет (≤768px)
- Вертикальное расположение элементов
- Размер таймера: 36px
- Центрированное выравнивание

### Мобильный (≤480px)
- Компактное отображение
- Оптимизированные размеры
- Сохранение читаемости

## 🚀 Как использовать

1. **Откройте страницу**: `http://localhost:3000/stats.html`
2. **Наблюдайте таймер**: время начинает накапливаться при активности
3. **Следите за статусом**: зеленый = активен, красный = неактивен
4. **Проверьте сохранение**: данные сохраняются автоматически

## 🔍 Отладка

### В консоли браузера:
```
🚀 Инициализация TimeTracker...
📊 Загружены данные: 0м, 0 очков
✅ TimeTracker инициализирован
🟢 Пользователь стал активен
🕐 Активность: 1м, 1 очков
💾 Данные сохранены: 1м, 1 очков
```

### Кнопки отладки:
- **"Тест"** - добавляет +1 минуту и +1 очко
- **"Сброс"** - очищает всю статистику

## 🎯 Преимущества

1. **Визуальная обратная связь** - пользователь видит свой прогресс
2. **Мотивация** - живой таймер стимулирует активность
3. **Прозрачность** - четкое понимание времени на сайте
4. **Красивый дизайн** - привлекательный интерфейс
5. **Реальное время** - обновление каждую секунду

## 🔧 Настройка

### Изменение цветов
```css
.live-timer {
    background: linear-gradient(135deg, #30CAA1 0%, #20A0FF 100%);
}
```

### Изменение размера таймера
```css
.live-timer__time {
    font-size: 48px; /* Измените размер */
}
```

### Отключение анимаций
```css
.live-timer {
    animation: none; /* Убрать пульсацию */
}
```

## 📊 Интеграция

Таймер интегрирован с существующей системой статистики:
- Использует те же данные `localStorage`
- Синхронизирован с `TimeTracker` классом
- Сохраняется вместе с общей статистикой
- Работает с системой достижений
